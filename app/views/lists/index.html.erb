<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
    <%= cl_image_tag '@list.uploaded_picture',
width: 150, height: 150, crop: :thumb, gravity: :face %>
  <% end %>
</ul>
